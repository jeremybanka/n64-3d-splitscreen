# Zig 0.16.0

Open `language-reference-0.16.0.html` in a browser for the self-contained
language reference. The installed mise toolchain also ships standard-library
source and can serve its generated API documentation with:

```sh
mise exec -- zig std
```

Upstream: <https://ziglang.org/documentation/0.16.0/>
